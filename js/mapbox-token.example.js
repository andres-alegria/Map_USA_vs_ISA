// Copy this file to js/mapbox-token.js and paste a Mapbox public token (pk.…).
// js/mapbox-token.js is git-ignored so the token never enters the repo.
// On Vercel, scripts/build-site.sh writes it from the MAPBOX_ACCESS_TOKEN variable instead.
window.MAPBOX_ACCESS_TOKEN = '';
